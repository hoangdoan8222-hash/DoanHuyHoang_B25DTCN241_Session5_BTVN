#include <stdio.h>
#include <math.h>

int main() {
    float a, b;
    char op;

    printf("Nhap so thu nhat (a): ");
    scanf("%f", &a);

    printf("Nhap so thu hai (b): ");
    scanf("%f", &b);

    printf("Nhap toan tu (+, -, *, /): ");
    scanf(" %c", &op); 
    
     return 0 ;
}
